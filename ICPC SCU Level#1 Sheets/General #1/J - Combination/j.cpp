#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main () {
  int n; cin >> n;
  int cards[n][2];
  int counter = 1;
  for (int i = 0; i < n; i++) {
    for (int j = 0; j < 2;) {
      cin >> cards[i][j];
    }
  }

  for (int i = 0; i < counter; i++) {
    
  }

}